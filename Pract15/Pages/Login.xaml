<Page x:Class="Pract15.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:Pract15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1100"
      Title="Login">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добро пожаловать в сомнительный магазин!" Style="{StaticResource Title}"/>

        <Button Grid.Row="1"  Width="500" Click="ButtonLoginCustomer_Click">
            <TextBlock Text="ВОЙТИ КАК ПОСЕТИТЕЛЬ" Style="{StaticResource WHITE}"/>
        </Button>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <TextBlock Text="Пин-код:" Style="{StaticResource MAIN}"/>
            <TextBox   Text="{Binding pinCode, UpdateSourceTrigger=PropertyChanged}"
                       materialDesign:HintAssist.Hint="Введите пин-код"
                       Style="{StaticResource Sub}"/>
            <Button Grid.Row="1"  Width="80" Height="50" Click="ButtonLoginAdmin_Click">
                <TextBlock Text="ОК" Style="{StaticResource WHITE}"/>
            </Button>
        </StackPanel>
    </Grid>
</Page>
