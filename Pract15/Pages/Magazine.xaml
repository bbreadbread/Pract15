<Page x:Class="Pract15.Pages.Magazine"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Pract15.Pages"
      xmlns:services="clr-namespace:Pract15.Service"
      mc:Ignorable="d" 
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      Title="Каталог" d:DesignHeight="750" d:DesignWidth="1100" Loaded="LoadList">

    <Page.Resources>
        <services:CategoriesService x:Key="catService"/>
        <services:BrandsService    x:Key="brdService"/>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="120"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Каталог" Style="{StaticResource Title}" Grid.ColumnSpan="3"/>

        <Button Content="&lt; На страницу входа" Grid.Column="0" Style="{StaticResource AdminBut}" Click="ButtonBack_Click"/>
        <Button x:Name="ButtonToAdminPage" Content="На панель администратора >" Grid.Column="3" Style="{StaticResource AdminBut}" Click="ButtonToAdminPage_Click" Visibility="Collapsed"/>
        
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
            <Label>Поиск</Label>
            <TextBox  Text="{Binding searchQuery, UpdateSourceTrigger=PropertyChanged}" 
                  TextChanged="TextBox_TextChanged"
                  materialDesign:HintAssist.Hint="Введите текст для поиска"
                  Style="{StaticResource Main}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="0">
            <Label>Сортировка</Label>
            <ComboBox SelectionChanged="ComboBoxSort_SelectionChanged"
                  Style="{StaticResource MainComboBox}">
                <ComboBox.Items>
                    <ComboBoxItem Tag="Name">По названию</ComboBoxItem>

                    <ComboBoxItem Tag="Price">
                        <StackPanel>
                            <TextBlock Text="Цена"/>
                            <ComboBox x:Name="PriceSort"
                                  Style="{StaticResource SubComboBox}">
                                <ComboBox.Items>
                                    <ComboBoxItem Tag="Descending">По убыванию</ComboBoxItem>
                                    <ComboBoxItem Tag="Ascending">По возрастанию</ComboBoxItem>
                                </ComboBox.Items>
                            </ComboBox>
                        </StackPanel>
                    </ComboBoxItem>

                    <ComboBoxItem Tag="Stock">
                        <StackPanel>
                            <TextBlock Text="Остаток"/>
                            <ComboBox x:Name="StockSort"
                                  Style="{StaticResource SubComboBox}">
                                <ComboBox.Items>
                                    <ComboBoxItem Tag="Descending">По убыванию</ComboBoxItem>
                                    <ComboBoxItem Tag="Ascending">По возрастанию</ComboBoxItem>
                                </ComboBox.Items>
                            </ComboBox>
                        </StackPanel>
                    </ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>
            <Label>Фильтры</Label>
            <ComboBox SelectionChanged="ComboBoxFilter_SelectionChanged"
                  Style="{StaticResource MainComboBox}">
                <ComboBox.Items>
                    <ComboBoxItem Tag="Category">По категории</ComboBoxItem>
                    <ComboBoxItem Tag="Brand">По бренду</ComboBoxItem>
                    <ComboBoxItem Tag="Price">По цене</ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>

            <StackPanel x:Name="FilterBrandPanel" Visibility="Collapsed">
                <Label FontSize="16">Фильтр по бренду</Label>
                <TextBox Text="{Binding filterBrand, UpdateSourceTrigger=PropertyChanged}"
             materialDesign:HintAssist.Hint="Введите бренд"
             TextChanged="TextBox_TextChanged"
                      Style="{StaticResource Main}"/>


                                    <StackPanel Grid.Column="1">
                                        <TextBlock Style="{DynamicResource WHITE}">Бренд:</TextBlock>
                                        <ComboBox DisplayMemberPath="Name"
                                  SelectedValuePath="Id"
                                  x:Name="cbBrand"
                                  SelectedValue="{Binding BrandId}"
                                  ItemsSource="{Binding Source={StaticResource brdService}, Path=Items}"
                                          SelectionChanged="cbBrand_SelectionChanged"        />
                                    </StackPanel>
                
            </StackPanel>

            <StackPanel x:Name="FilterCategoryPanel" Visibility="Collapsed">
                <Label FontSize="16">Фильтр по категории</Label>
                <TextBox Text="{Binding filterCategory, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="Введите категорию"
                 TextChanged="TextBox_TextChanged"
                          Style="{StaticResource Main}"/>



                                    <StackPanel Grid.Column="1">
                                        <TextBlock Style="{DynamicResource WHITE}">Категория:</TextBlock>
                                        <ComboBox DisplayMemberPath="Name"
                                  SelectedValuePath="Id"
                                  x:Name="cbCat"
                                  SelectedValue="{Binding CategoryId}"
                                  ItemsSource="{Binding Source={StaticResource catService}, Path=Items}"
                                                  SelectionChanged="cbCat_SelectionChanged"/>
                                        
                                    </StackPanel>
                
            </StackPanel>            <StackPanel x:Name="FilterPricePanel" Visibility="Collapsed">
                <Label FontSize="16">Фильтр по цене</Label>
                <Label>от</Label>
                <TextBox Text="{Binding filterPriceIdFrom, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="От"
                     TextChanged="TextBox_TextChanged"
                      Style="{StaticResource Main}"/>
                <Label>до</Label>
                <TextBox Text="{Binding filterPriceIdTo, UpdateSourceTrigger=PropertyChanged}" 
                    materialDesign:HintAssist.Hint="До"
                    TextChanged="TextBox_TextChanged"
                      Style="{StaticResource Main}"/>
            </StackPanel>            <Button Margin="5" Click="Button_Click">
                <TextBlock Text="Сбросить" Style="{DynamicResource WHITE}"/>
            </Button>
        </StackPanel>
        <materialDesign:ColorZone Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Mode="SecondaryMid">
            <ListView Grid.Row="1" Grid.Column="1" ItemsSource="{Binding products}" Name="FormsList" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ProductBorder" Style="{StaticResource Stock}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" >
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource WHITE}"/>
                                    <TextBlock Style="{StaticResource WHITE}">
                                        <Run Text="★ "/>
                                        <Run Text="{Binding Rating}"/>
                                    </TextBlock>

                                    <TextBlock Style="{StaticResource WHITE}">
                                        <Run Text="{Binding Category.Name}"/>
                                    </TextBlock>

                                    <TextBlock Style="{StaticResource WHITE}">
                                        <Run Text="{Binding Description}"/>
                                    </TextBlock>

                                    <ItemsControl ItemsSource="{Binding ProductTags}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Style="{StaticResource WHITE}">
                                                    <Run Text="#"/>
                                                    <Run Text="{Binding Tag.Name}"/>
                                                    <Run Text=" "/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock HorizontalAlignment="Right" Style="{StaticResource WHITE}">
                                        <Run Text="{Binding Price}"/>
                                        <Run Text=" Рублей"/>
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Right" Style="{StaticResource WHITE}">
                                        <Run Text="Остаток "/>
                                        <Run Text="{Binding Stock}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Stock}" Value="9">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="8">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="7">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="6">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="5">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="4">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="3">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="2">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="1">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Stock}" Value="0">
                                <Setter TargetName="ProductBorder" Property="BorderBrush" Value="{StaticResource BalanceBorder}"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </materialDesign:ColorZone>
        
        <WrapPanel Grid.Row="3" Grid.ColumnSpan="3">
            <TextBlock Style="{StaticResource WHITE}" Text="{Binding forms.Count, StringFormat='{}Всего записей:{0}'}"/>
            <TextBlock Style="{StaticResource WHITE}" Text="{Binding Items.Count, ElementName=FormsList, StringFormat='{} Показано: {0}'}"/>
        </WrapPanel>    </Grid>
</Page>
